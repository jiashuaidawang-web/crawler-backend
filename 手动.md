# ========== JobController (/api/job) ==========

# 1. stock_daily —— 按页拆任务(每页100条)
curl -X POST "http://localhost:8081/api/job/seed-stock-daily" -d "date=2026-08-07&source=1"

# 2. board_basic —— 地域/行业/概念板块基础维表
curl -X POST "http://localhost:8081/api/job/seed-board-basic" -d "date=2026-08-07&source=1"

# 3. board_daily —— 地域/行业/概念板块日线
curl -X POST "http://localhost:8081/api/job/seed-board-daily" -d "date=2026-08-07&source=1"

# 4. limit_up_pool / limit_down_pool / zhaban_pool / strong_pool / cixin_pool(5个池子一起)
curl -X POST "http://localhost:8081/api/job/seed-pools-only" -d "date=2026-08-07&source=1"

# 5. stock_board_rel —— 板块-个股关联(依赖 board_basic)
curl -X POST "http://localhost:8081/api/job/sync-board-relations" -d "date=2026-08-07&source=1"

# 6. dragon_tiger —— 龙虎榜主表
curl -X POST "http://localhost:8081/api/job/chain-dragon-details" -d "date=2026-08-07"

# 7. dt_detail —— 龙虎榜明细(依赖 dragon_tiger)
curl -X POST "http://localhost:8081/api/job/chain-dragon-details" -d "date=2026-08-07"


# ========== SeedController (/api/crawl) ==========

# 8. concept —— 从 board_basic 派生概念维表
curl -X POST "http://localhost:8081/api/crawl/seed-concept"

# 9. trade_calendar —— 交易日历
curl -X POST "http://localhost:8081/api/crawl/seed-trade-calendar"

# 10. northbound_flow —— 北向资金
curl -X POST "http://localhost:8081/api/crawl/seed-northbound" -d "date=2026-08-07&source=1"

# 11. dragon_tiger —— 龙虎榜主表(另一入口,发抓取任务)
curl -X POST "http://localhost:8081/api/crawl/seed-dragon-tiger" -d "date=2026-08-07&source=1"

# 12. financial —— 财报
curl -X POST "http://localhost:8081/api/crawl/seed-financial" -H "Content-Type: application/json" -d '{"source":0,"tsCodes":["600000.SH"]}'

# 13. news_event —— 新闻事件
curl -X POST "http://localhost:8081/api/crawl/seed-news-event" -H "Content-Type: application/json" -d '{"source":0,"limit":200}'


# ========== 通用下发(/api/crawl/seed,JSON) ==========
# 这些是"发任务 → worker 认领 → 落表"的异步链路:
# stock_weekly / index_daily / main_fund_flow / sentiment_daily / theme_factor_daily 等

# 14. stock_weekly —— 周线
curl -X POST "http://localhost:8081/api/crawl/seed" -H "Content-Type: application/json" -d '{"taskType":"STOCK_WEEKLY","tradeDate":"2026-08-07","source":1}'

# 15. index_daily —— 指数日线
curl -X POST "http://localhost:8081/api/crawl/seed" -H "Content-Type: application/json" -d '{"taskType":"INDEX_DAILY","tradeDate":"2026-08-07","source":1}'

# 16. main_fund_flow —— 主力资金流
curl -X POST "http://localhost:8081/api/crawl/seed" -H "Content-Type: application/json" -d '{"taskType":"MAIN_FUND_FLOW","tradeDate":"2026-08-07","source":1}'