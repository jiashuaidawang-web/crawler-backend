<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.dunwugudao</groupId>
    <artifactId>crawler-backend</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>
    <name>crawler-backend</name>
    <description>《顿悟股道》股票数据分布式爬虫底座 (M1 脚手架)</description>

    <modules>
        <module>crawler-core</module>
        <module>crawler-strategy</module>
        <module>crawler-persistence</module>
        <module>crawler-worker</module>
        <module>crawler-admin</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <mybatis-spring-boot.version>3.0.3</mybatis-spring-boot.version>
        <okhttp.version>4.12.0</okhttp.version>
        <playwright.version>1.40.0</playwright.version>
        <postgresql.version>42.7.3</postgresql.version>
        <clickhouse-jdbc.version>0.6.5</clickhouse-jdbc.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- 原生 MyBatis Spring Boot（替代 mybatis-plus-spring-boot3-starter，CK 无 MP 方言） -->
            <dependency>
                <groupId>org.mybatis.spring.boot</groupId>
                <artifactId>mybatis-spring-boot-starter</artifactId>
                <version>${mybatis-spring-boot.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-annotation</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>okhttp</artifactId>
                <version>${okhttp.version}</version>
            </dependency>
            <dependency>
                <groupId>com.microsoft.playwright</groupId>
                <artifactId>playwright</artifactId>
                <version>${playwright.version}</version>
            </dependency>
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>
            <dependency>
                <groupId>com.clickhouse</groupId>
                <artifactId>clickhouse-jdbc</artifactId>
                <version>${clickhouse-jdbc.version}</version>
            </dependency>
            <!-- clickhouse-jdbc 0.6.x 默认用 Apache HTTP 客户端；显式声明避免 ClassNotFoundException -->
            <dependency>
                <groupId>org.apache.httpcomponents.client5</groupId>
                <artifactId>httpclient5</artifactId>
                <version>5.3.1</version>
            </dependency>
            <!-- LZ4 压缩（clickhouse 数据压缩用） -->
            <dependency>
                <groupId>org.lz4</groupId>
                <artifactId>lz4-java</artifactId>
                <version>1.8.0</version>
            </dependency>
            <dependency>
                <groupId>com.xuxueli</groupId>
                <artifactId>xxl-job-core</artifactId>
                <version>2.4.0</version>
            </dependency>
            <!-- 注意：lombok 版本由 spring-boot-starter-parent 的 BOM 统一管理，
                 子模块直接 <scope>provided</scope> 引用即可，无需在此声明版本 -->
        </dependencies>
    </dependencyManagement>
</project>
