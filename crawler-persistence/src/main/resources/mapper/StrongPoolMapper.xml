<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.StrongPoolMapper">

    <!-- ClickHouse MergeTree 批量追加 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO strong_pool
        (
        trade_date,
        ts_code,
        stock_name,
        latest_price,
        pct_chg,
        ztp,
        zs,
        nh,
        board_pos,
        lb,
        amount,
        ltsz,
        tshare,
        turnover_rate,
        board_code,
        zttj_ct,
        zttj_days,
        data_source,
        src_detail,
        create_date,
        update_date
        )
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tradeDate},
             #{r.tsCode},
             #{r.stockName},
             #{r.latestPrice},
             #{r.pctChg},
             #{r.ztp},
             #{r.zs},
             #{r.nh},
             #{r.boardPos},
             #{r.lb},
             #{r.amount},
             #{r.ltsz},
             #{r.tshare},
             #{r.turnoverRate},
             #{r.boardCode},
             #{r.zttjCt},
             #{r.zttjDays},
             #{r.dataSource},
             #{r.srcDetail},
             #{r.createDate},
             #{r.updateDate}
            )
        </foreach>
    </insert>

    <select id="selectDistinctTsCodeAndName" resultType="java.util.Map">
        SELECT DISTINCT ts_code, stock_name
        FROM strong_pool
        WHERE ts_code IS NOT NULL AND stock_name IS NOT NULL
    </select>

</mapper>
