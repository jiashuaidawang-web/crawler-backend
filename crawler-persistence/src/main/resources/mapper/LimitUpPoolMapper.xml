<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.LimitUpPoolMapper">

    <!-- ClickHouse MergeTree 批量追加 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO limit_up_pool
        (
        trade_date,
        ts_code,
        stock_name,
        latest_price,
        pct_chg,
        board_pos,
        is_first,
        is_continuous,
        limit_style,
        open_time,
        last_time,
        open_times,
        fund,
        amount,
        ltsz,
        tshare,
        turnover_rate,
        board_code,
        zttj_ct,
        zttj_days,
        data_source,
        src_detail,
        create_date,
        update_date
        )
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tradeDate},
             #{r.tsCode},
             #{r.stockName},
             #{r.latestPrice},
             #{r.pctChg},
             #{r.boardPos},
             #{r.isFirst},
             #{r.isContinuous},
             #{r.limitStyle},
             #{r.openTime},
             #{r.lastTime},
             #{r.openTimes},
             #{r.fund},
             #{r.amount},
             #{r.ltsz},
             #{r.tshare},
             #{r.turnoverRate},
             #{r.boardCode},
             #{r.zttjCt},
             #{r.zttjDays},
             #{r.dataSource},
             #{r.srcDetail},
             #{r.createDate},
             #{r.updateDate}
            )
        </foreach>
    </insert>

</mapper>
