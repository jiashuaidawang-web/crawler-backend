<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.StockBoardRelMapper">

    <!-- 批量追加（去重由 ReplacingMergeTree(_ver=data_source) 承接） -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO stock_board_rel
        (ts_code, board_code, board_name, stock_name, board_type, is_leader, is_midarm, weight,
         effective_date, data_source, src_detail, create_date, update_date)
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tsCode}, #{r.boardCode}, #{r.boardName}, #{r.stockName}, #{r.boardType},
             #{r.isLeader}, #{r.isMidarm}, #{r.weight}, #{r.effectiveDate}, #{r.dataSource},
             #{r.srcDetail}, #{r.createDate}, #{r.updateDate}
            )
        </foreach>
    </insert>

</mapper>
