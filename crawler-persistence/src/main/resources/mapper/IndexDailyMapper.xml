<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.IndexDailyMapper">

    <!-- ClickHouse MergeTree 批量追加 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO index_daily
        (
        trade_date,
        index_code,
        index_name,
        open,
        high,
        low,
        close,
        pre_close,
        pct_chg,
        vol,
        amount,
        turnover,
        data_source,
        src_detail,
        create_date,
        update_date
        )
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tradeDate},
             #{r.indexCode},
             #{r.indexName},
             #{r.open},
             #{r.high},
             #{r.low},
             #{r.close},
             #{r.preClose},
             #{r.pctChg},
             #{r.vol},
             #{r.amount},
             #{r.turnover},
             #{r.dataSource},
             #{r.srcDetail},
             #{r.createDate},
             #{r.updateDate}
            )
        </foreach>
    </insert>

</mapper>
