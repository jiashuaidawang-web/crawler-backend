<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//MyBatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.DtDetailMapper">

    <!-- ClickHouse MergeTree 批量追加 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO dt_detail
        (
        trade_date, ts_code, seat_name, seat_type, rank,
        buy, sell, net_buy, buy_ratio, sell_ratio, net_buy_ratio,
        trade_amt, trade_ratio, accum_volume, accum_amount,
        change_rate, turnoverrate_ratio, trade_direction, statistics_days, onlist_times,
        start_date, end_date, operate_dept_code, operate_dept_type,
        change_type, explanation, trade_id, security_inner_code, sec_type,
        data_source, src_detail, create_date, update_date
        )
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tradeDate}, #{r.tsCode}, #{r.seatName}, #{r.seatType}, #{r.rank},
             #{r.buy}, #{r.sell}, #{r.netBuy}, #{r.buyRatio}, #{r.sellRatio}, #{r.netBuyRatio},
             #{r.tradeAmt}, #{r.tradeRatio}, #{r.accumVolume}, #{r.accumAmount},
             #{r.changeRate}, #{r.turnoverrateRatio}, #{r.tradeDirection}, #{r.statisticsDays}, #{r.onlistTimes},
             #{r.startDate}, #{r.endDate}, #{r.operateDeptCode}, #{r.operateDeptType},
             #{r.changeType}, #{r.explanation}, #{r.tradeId}, #{r.securityInnerCode}, #{r.secType},
             #{r.dataSource}, #{r.srcDetail}, #{r.createDate}, #{r.updateDate}
            )
        </foreach>
    </insert>

    <!-- 查某交易日所有上榜 TRADE_ID（去重，供 chainSeatDetails 下发子任务用） -->
    <select id="selectDistinctTradeIds" resultType="java.lang.Long">
        SELECT DISTINCT trade_id FROM dt_detail WHERE trade_date = #{tradeDate} AND trade_id IS NOT NULL
    </select>
</mapper>
