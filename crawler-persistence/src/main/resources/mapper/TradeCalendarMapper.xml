<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.TradeCalendarMapper">

    <!-- ClickHouse ReplacingMergeTree 批量追加：同 trade_date 重复 seed 由 _ver 保留最新版，幂等 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO trade_calendar
        (
        trade_date,
        is_trading,
        data_source,
        src_detail,
        create_date,
        update_date
        )
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tradeDate},
             #{r.isTrading},
             #{r.dataSource},
             #{r.srcDetail},
             #{r.createDate},
             #{r.updateDate}
            )
        </foreach>
    </insert>

    <!-- 前一交易日：严格小于 date 的最大交易日（ReplacingMergeTree 需 FINAL 去重旧版本） -->
    <select id="findPrevTradingDay" resultType="java.time.LocalDate">
        SELECT MAX(trade_date)
        FROM trade_calendar FINAL
        WHERE trade_date &lt; #{date}
          AND is_trading = 1
    </select>

    <!-- 是否交易日 -->
    <select id="isTradingDay" resultType="boolean">
        SELECT COUNT(1) &gt; 0
        FROM trade_calendar FINAL
        WHERE trade_date = #{date}
          AND is_trading = 1
    </select>

    <!-- 区间交易日升序 -->
    <select id="selectTradingDaysBetween" resultType="java.time.LocalDate">
        SELECT trade_date
        FROM trade_calendar FINAL
        WHERE trade_date BETWEEN #{from} AND #{to}
          AND is_trading = 1
        ORDER BY trade_date ASC
    </select>

</mapper>
