<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.StockWeeklyMapper">

    <!-- ClickHouse MergeTree 批量追加 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO stock_weekly
        (
        trade_date,
        ts_code,
        stock_name,
        open,
        high,
        low,
        close,
        vol,
        amount,
        data_source,
        src_detail,
        create_date,
        update_date,
        chg_amount,
        amplitude,
        volume_ratio,
        avg_price,
        main_net,
        pe_static,
        leader_code,
        industry_code,
        concept_code,
        market_code
        )
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tradeDate},
             #{r.tsCode},
             #{r.stockName},
             #{r.open},
             #{r.high},
             #{r.low},
             #{r.close},
             #{r.vol},
             #{r.amount},
             #{r.dataSource},
             #{r.srcDetail},
             #{r.createDate},
             #{r.updateDate},
             #{r.chgAmount},
             #{r.amplitude},
             #{r.volumeRatio},
             #{r.avgPrice},
             #{r.mainNet},
             #{r.peStatic},
             #{r.leaderCode},
             #{r.industryCode},
             #{r.conceptCode},
             #{r.marketCode}
            )
        </foreach>
    </insert>

</mapper>
