<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.MainFundFlowMapper">

    <!-- ClickHouse MergeTree 批量追加 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO main_fund_flow
        (
        trade_date,
        obj_type,
        ts_code,
        board_code,
        index_code,
        main_net,
        super_big,
        big_net,
        mid_net,
        small_net,
        data_source,
        src_detail,
        create_date,
        update_date
        )
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tradeDate},
             #{r.objType},
             #{r.tsCode},
             #{r.boardCode},
             #{r.indexCode},
             #{r.mainNet},
             #{r.superBig},
             #{r.bigNet},
             #{r.midNet},
             #{r.smallNet},
             #{r.dataSource},
             #{r.srcDetail},
             #{r.createDate},
             #{r.updateDate}
            )
        </foreach>
    </insert>

</mapper>
