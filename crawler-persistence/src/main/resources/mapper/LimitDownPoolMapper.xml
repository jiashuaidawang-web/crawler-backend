<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.LimitDownPoolMapper">

    <!-- ClickHouse MergeTree 批量追加 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO limit_down_pool
        (
        trade_date,
        ts_code,
        stock_name,
        latest_price,
        pct_chg,
        pe,
        fund,
        last_time,
        fba,
        days,
        oc,
        amount,
        ltsz,
        tshare,
        turnover_rate,
        board_code,
        data_source,
        src_detail,
        create_date,
        update_date
        )
        VALUES
        <foreach collection="list" item="r" separator=",">
            (
             #{r.tradeDate},
             #{r.tsCode},
             #{r.stockName},
             #{r.latestPrice},
             #{r.pctChg},
             #{r.pe},
             #{r.fund},
             #{r.lastTime},
             #{r.fba},
             #{r.days},
             #{r.oc},
             #{r.amount},
             #{r.ltsz},
             #{r.tshare},
             #{r.turnoverRate},
             #{r.boardCode},
             #{r.dataSource},
             #{r.srcDetail},
             #{r.createDate},
             #{r.updateDate}
            )
        </foreach>
    </insert>

</mapper>
