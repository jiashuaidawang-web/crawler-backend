<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.crawler.persistence.mapper.StockDailyMapper">

    <!--
        个股日线批量追加（ClickHouse MergeTree 纯追加）。
        注意：ClickHouse JDBC 0.6.x 不支持 {@code InsertGenerator} 风格的批量，
        这里用 UNION ALL 方式拼多行 VALUES，单次建议 ≤ 1000 行（由调用方切片）。
    -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO stock_daily
        (trade_date, ts_code, stock_name, open, high, low, close, pre_close, pct_chg, vol, amount,
         turnover, total_mv, circ_mv, pe, is_limit_up, is_limit_down, chg_amount, amplitude,
         volume_ratio, avg_price, main_net, pe_static, leader_code, industry_code, concept_code,
         market_code, reserved_f24, reserved_f25, reserved_f107, reserved_f136, reserved_f173,
         velocity, is_new_high, chg_60d, seal_fund, board_days, board_stat, first_seal_time,
         last_seal_time, limit_type, data_source, src_detail, create_date, update_date)
        VALUES
        <foreach collection="list" item="r" separator=",">
            (#{r.tradeDate}, #{r.tsCode}, #{r.stockName}, #{r.open}, #{r.high}, #{r.low}, #{r.close},
             #{r.preClose}, #{r.pctChg}, #{r.vol}, #{r.amount}, #{r.turnover}, #{r.totalMv}, #{r.circMv},
             #{r.pe}, #{r.isLimitUp}, #{r.isLimitDown}, #{r.chgAmount}, #{r.amplitude}, #{r.volumeRatio},
             #{r.avgPrice}, #{r.mainNet}, #{r.peStatic}, #{r.leaderCode}, #{r.industryCode}, #{r.conceptCode},
             #{r.marketCode}, #{r.reservedF24}, #{r.reservedF25}, #{r.reservedF107}, #{r.reservedF136},
             #{r.reservedF173}, #{r.velocity}, #{r.isNewHigh}, #{r.chg60d}, #{r.sealFund}, #{r.boardDays},
             #{r.boardStat}, #{r.firstSealTime}, #{r.lastSealTime}, #{r.limitType}, #{r.dataSource},
             #{r.srcDetail}, #{r.createDate}, #{r.updateDate})
        </foreach>
    </insert>

</mapper>
