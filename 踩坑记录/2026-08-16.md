# 2026-08-16 踩坑记录

## Worker 调度线程卡死 + 熔断器开启导致任务完全停滞

### 问题现象
- `worker.log` 自 **2026-08-15 18:11:44** 后完全停止写入(停滞 6 小时+)
- 但 `info.log` / `error.log` 持续写到 00:53(说明 admin/web 层活着,worker 调度层死了)
- 触发任务: `STOCK_BY_BOARD`(task=21468),目标 `83.push2.eastmoney.com/api/qt/clist/get`(板块 bk1547)

### 根因(按影响排序)
1. **代理池连续失败 50/50 次** → WorkerProxyManager 熔断器开启,600 秒冷却
2. **熔断期间 url 级快速失败**,但 `backoff` 仍按 2s→4s→8s→16s→30s 指数退避,任务 21468 烧满 10/10 个 IP 全 502
3. **线程疑似卡死**:熔断器 600s 到期后(18:21 后)日志仍无任何新写入,worker 调度线程(`scheduling-1`)未恢复,说明 `ClaimLoop` 可能因异常退出或死锁

### 关键日志
- `18:08:42.709 WARN 已获取 50 次新 IP,超过上限 50,触发保护性停止`
- `18:08:42.709 ERROR ⚠️熔断器开启! 连续失败 50 次, 600000ms 内不再提取新代理`
- `18:11:44.889 WARN proxy failed, usedIp=10/10, backoff 30000ms`(最后一条)
- task=21477 重试到 2/3(`RETRY retryCount=2/3`)

### 文件位置
- `crawler-worker/.../scheduler/ClaimLoop.java:67,94` — 调度循环
- `crawler-worker/.../eastmoney/WorkerProxyManager.java` — 熔断器逻辑(50 次上限、600s 冷却)
- `crawler-worker/.../eastmoney/EastmoneyApiStrategy.java:83` — fetch 入口

### 修复(待排查)
- [ ] 确认 worker 调度线程是否存活(jstack / 线程 dump)
- [ ] 熔断器恢复后 ClaimLoop 是否自动恢复?否则需加心跳/看门狗
- [ ] 502 全量失败时代理池健康检查是否该提前触发换池

### 关键排查经验
> **worker.log 停滞 ≠ 服务宕机**: admin/web 层(info.log)可能仍在跑,要对比多日志文件的 mtime 判断哪一层卡死。熔断器冷却期结束后任务仍不恢复,说明调度线程本身异常退出,需看门狗拉起。

### 踩坑索引
| 坑 | 影响 | 修复状态 |
|---|---|---|
| 熔断器 600s 冷却期内任务全失败 | 单任务烧满 10 IP | 已理解 |
| 熔断器恢复后调度线程未重启 | 全 worker 停滞 6h+ | **待排查** |
